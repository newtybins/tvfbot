export default {
	name: '',
    description: '',
    aliases: [''],
    permissions: [''],
    args: false,
    usage: '',
    staffAccess: [''],
	allowGeneral: false,
	run: async (tvf, msg, args, { prefix }) => {
		// insert code here
	}
} as Command;
