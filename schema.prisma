generator client {
  provider      = "prisma-client-js"
  binaryTargets = ["native"]
}

datasource db {
  provider = "postgresql"
  url      = "postgres://tvfbot:7pcSbFCeSiQbWJ@132.145.35.59:5432/tvf"
}

model User {
  id    String @id(map: "user_pk") @unique(map: "user_id_uindex")
  level Int    @default(1)
  xp    Int    @default(0)
}
